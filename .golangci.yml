run:
  timeout: 5m

linters:
  anable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - misspell
  
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck